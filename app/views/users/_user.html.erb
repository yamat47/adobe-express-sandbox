<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Avatar:</strong>
    <%= image_tag user.avatar, width: 100 if user.avatar.attached? %>
  </p>

</div>
